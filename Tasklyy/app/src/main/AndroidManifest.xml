<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for Firebase & Google Sign-In -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tasklyy"
        tools:targetApi="31">

        <activity
            android:name=".SplashScreen.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddTaskScreen.AddTaskScreen" />


        <!-- Login Screen -->
        <activity
            android:name=".AuthenticationScreens.LoginScreen.LoginActivity"
            android:exported="true"
            />
        <!-- SignUpScreen -->
        <activity
            android:name=".AuthenticationScreens.SignUpScreen.SignUpActivity"
            android:exported="true"
            />

        <!-- Main Screen -->
        <activity
            android:name=".HomeScreen.MainActivity"
            android:exported="true" />

    </application>

</manifest>
